# 律师工作台 3.0

以破产案件为首个领域的律师工作台，以 skill 包形式交付。律师在案件工作区内推进一个个节点，由节点补全模块，由模块补全图。

本文件是 3.0 的权威词汇表。`knowledge/` 下 2.0 沉淀的资料若使用同名词而含义不同，以本文件为准（见 ADR-0001）。

## 流程

**节点**：
流程的最小单元。每个节点恰有一份文书，每份文书对应一个节点；同时递交的多份文书是同一模块下的多个节点，"同出"不是节点的属性。实物动作（接管、张贴、签收）不是节点，只作为某份文书的要件。节点由对其文书的动作推进：生成、确认。状态只有三个正向态与一个终态：未生成、已生成、已确认、不适用；确认即完成。已确认后重出，节点回到已生成，旧的确认留在历史里。节点另有一个与状态正交的属性：有没有空白模板。
_Avoid_：任务、步骤、小节、文书节点、递交事件、审批、阻塞、待材料

**模块**：
若干节点的集合。模块及其中的节点可由开发者或用户增删改，遵循惰性原则。模块可以从指南和手册初步产出，也可以由 agent 按案件类型初步产出，也可以由开发者或用户用自然语言描述产出。
_Avoid_：章节、阶段、模块序号、固定 12 模块

**图**：
当前案件中节点的全集及其状态，状态包括节点的排序和模块的排序。图代表该案件已有的流程；节点和模块的不断补充使图逐渐完整。图也是案件唯一的台账：律师的每个动作是图里对应节点上的一条带来源的条目，agent 永不改动律师的条目。律师不直接读图，读由图派生的视图。
_Avoid_：流程模型、工作流、看板、快照、登记表、节点标记、批准记录、分支适用

**惰性原则**：
节点与模块只在被需要时才创建或补全，不预先铺满整个流程。
_Avoid_：预置全流程、全量初始化

**条目**：
图里某个节点上的一条记录，记一个动作、发生时间、来源与该动作带出的事实（如一次生成产出的文书与审查报告）。条目只增，从不修改或删除；来源由动作类型决定：确认与不适用来自律师，生成来自 agent（律师兜底的那份由 agent 登记，来源记为律师）。节点状态由其条目推出，不另行记录。
_Avoid_：记录行、日志、事件、标记、批注

**派生视图**：
由图生成、供律师阅读的文件，图每次变动后整体重算覆盖。它不是台账，不承载任何图里没有的事实。
_Avoid_：快照、看板、报表、台账

**图存档**：
趋近完整的图被保存下来的版本，供路由参考，并为可视化提供数据。
_Avoid_：快照、导出

## 动作

**生成**：
agent 依据材料（可再加过往文书、文书提示词或空白模板之一）产出一版文书及其审查报告的动作，由律师发起。agent 效果不佳时律师自己写一份兜底，律师一句话，agent 登记为已生成。生成永不写入案件工作区里已存在的文书文件，每次都是新版本。
_Avoid_：起草、填充、draft

**确认**：
律师读审查报告后对一版文书拍板的动作，只能由律师发起，agent 永不代做。确认即节点完成。开发者在测试案件里做确认时以律师身份行事。
_Avoid_：批准、审批、两段式、出示与落盘

**不适用**：
律师宣告某个节点在本案不需要走的动作，节点进入终态。
_Avoid_：分支适用、跳过、删除节点

**初始化**：
案件开始时建立标准案件工作区的动作：创建目录层次，对已有的材料、模板、文书、指南做归档等预处理；没有材料时建好空目录。
_Avoid_：导入归档、setup

**路由**：
随时可调用的只读地图。根据用户提示词回答当前案件处于哪个节点、上一个已完成的任务、接下来的任务（含审批、确认、文书生成等），需要具体能力时指向对应 skill 并帮助编排提示词。路由不触发 skill。
_Avoid_：调度、入口、主命令

## 结构

**编排 skill**：
用户主动调用、驱动一段流程的 skill。构成编排层。
_Avoid_：主动 skill、命令 skill

**参考 skill**：
被编排 skill 或模型按需调用的工具类 skill。构成参考层。
_Avoid_：被动 skill、工具 skill、脚本

**文书**：
节点对应的那一份对外或对内成品。文书有版本，版本属于文书，不在节点上展示；每一版都以直接可提交为目标。
_Avoid_：文件、产物、工作副本、已批准

**审查报告**：
每一版文书必有的一份伴生文件，与文书一起落盘，缺一即生成失败。内容是对照要件与材料的审查结论与取值来源，是律师做确认的依据。
_Avoid_：审查文档、审查目录、取值台账

**空白模板**：
一个节点的文书骨架，来源有三：官方模板、从过往文书剔除案件内容、从材料反推。官方模板是来源为官方的空白模板。
_Avoid_：分离空白模板、标准模板、模板库

**律师陈述**：
律师本人录入的、作为事实来源的材料。性质三档：直接陈述、转述、裁定。案件级裁定记在这里，不另设台账。
_Avoid_：裁定台账、备注

**律师**：
在案件工作区里办案、发起确认与不适用的人。开发者是在本仓库里做开发会话的人，做律师动作时即是律师。
_Avoid_：用户、办案人员

**案件工作区**：
仓库外存放单个案件的全部材料、文书与图的目录。案件工作区永不进入本仓库。
_Avoid_：案件目录、案件材料包、案件仓库

**可视化插件**：
读取图存档、把图呈现给律师看的组件。
_Avoid_：工作台、看板、仪表盘
