# 律师工作台 3.0

以破产案件为首个领域的律师工作台，以 skill 包形式交付。律师在案件工作区内推进一个个节点，由节点补全模块，由模块补全图。

本文件是 3.0 的权威词汇表。`knowledge/` 下 2.0 沉淀的资料若使用同名词而含义不同，以本文件为准（见 ADR-0001）。

## 流程

**节点**：
流程的最小单元，每一份文书对应一个节点。节点由对其文书的动作推进：生成、确认、审批。
_Avoid_：任务、步骤、小节、文书节点

**模块**：
若干节点的集合。模块及其中的节点可由开发者或用户增删改，遵循惰性原则。模块可以从指南和手册初步产出，也可以由 agent 按案件类型初步产出，也可以由开发者或用户用自然语言描述产出。
_Avoid_：章节、阶段、模块序号、固定 12 模块

**图**：
当前案件中节点的全集及其状态，状态包括节点的排序和模块的排序。图代表该案件已有的流程；节点和模块的不断补充使图逐渐完整。
_Avoid_：流程模型、工作流、看板、快照

**惰性原则**：
节点与模块只在被需要时才创建或补全，不预先铺满整个流程。
_Avoid_：预置全流程、全量初始化

**图存档**：
趋近完整的图被保存下来的版本，供路由参考，并为可视化提供数据。
_Avoid_：快照、导出

## 动作

**初始化**：
案件开始时建立标准案件工作区的动作：创建目录层次，对已有的材料、模板、文书、指南做归档等预处理；没有材料时建好空目录。
_Avoid_：导入归档、setup

**路由**：
随时可调用的只读地图。根据用户提示词回答当前案件处于哪个节点、上一个已完成的任务、接下来的任务（含审批、确认、文书生成等），需要具体能力时指向对应 skill 并帮助编排提示词。路由不触发 skill。
_Avoid_：调度、入口、主命令

## 结构

**编排 skill**：
用户主动调用、驱动一段流程的 skill。构成编排层。
_Avoid_：主动 skill、命令 skill

**参考 skill**：
被编排 skill 或模型按需调用的工具类 skill。构成参考层。
_Avoid_：被动 skill、工具 skill、脚本

**案件工作区**：
仓库外存放单个案件的全部材料、文书与图的目录。案件工作区永不进入本仓库。
_Avoid_：案件目录、案件材料包、案件仓库

**可视化插件**：
读取图存档、把图呈现给律师看的组件。
_Avoid_：工作台、看板、仪表盘
