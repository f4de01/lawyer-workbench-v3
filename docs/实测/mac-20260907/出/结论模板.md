# 实测结论（macOS + Codex）

- 日期：
- 机器：（型号 / 芯片，`出/01-环境.txt` 里有）
- 填表的是：Mac 上的 Codex（客户端，**非** CLI）

> 填法：每行只填事实：是 / 否 / 报错原文 / 逐字抄回的字符串。
> 填不出来的写「未跑到（卡在第 N 步：…）」。不要留空，不要下结论，不要写建议。
> 任何路径里的家目录写成 `~`。

## 票 #40　安装链与 `$` 提及形态

| # | 问 | 答 | 原始回显在 |
|---|---|---|---|
| 1 | macOS 版本、芯片 | | `01-环境.txt` |
| 1 | `python3` 路径与版本 | | `01-环境.txt` |
| 2 | `python-docx` 能 import 吗 | | `02-依赖.txt` |
| 2 | `PyMuPDF` 能 import 吗 | | `02-依赖.txt` |
| 2 | 不能的话，装的时候发生了什么（挂起？报错？） | | |
| 3 | Word for Mac 版本与构建号 | | `03-Word.txt` |
| 4 | **走通的是哪一级路线**（抄 `04-安装.txt` 末尾那行） | | `04-安装.txt` |
| 4 | 第 1 级 `npx skills add` 结果（成功 / 卡交互 / 凭据失败 / 超时） | | `04-安装.txt` |
| 4 | `node` / `npx` / `codex` CLI 在不在（预期：都没有） | | `04-安装.txt` |
| 4 | 第 2 级 `gh clone` 走没走（本机 gh 登录了吗） | | `04-安装.txt` |
| 4 | 第 3 级拷目录走没走 | | `04-安装.txt` |
| 4 | 装到了哪个目录 | | `04-安装.txt` |
| 5 | 磁盘上六件 skill 的 `name` 字段，**逐字**抄在下面 | 见下表 | `05-清单.txt` |
| 5 | 目录名与 `name` 有没有不一致的 | | `05-清单.txt` |
| 6 | 两件编排 skill（`loo0ng-setup-case`、`loo0ng-doit`）在不在 | | `05-清单.txt` |
| 7 | **Skills 侧边栏**里显示的名字（律师逐字念的原话） | | 人工 |
| 7 | 侧边栏显示的与磁盘 `name` 一致吗；不一致的两个都写 | | 人工 + `05-清单.txt` |
| 8 | 客户端输入框打 `$` 之后补全列表显示什么（律师原话） | | 人工 |
| 8 | 打完整一个名字加「你好」，有反应吗 | | 人工 |

**第 5 / 7 问的逐字抄写**（一个字符都不要改）。两列分别来自磁盘与律师的眼睛，
不一致本身就是要报的事实，**不要挑一个填**：

| 开发机上的名字 | 磁盘 `SKILL.md` 的 `name`（`05-清单.txt`） | Skills 侧边栏 / `$` 补全显示的（律师念的） |
|---|---|
| `loo0ng-setup-case` | | |
| `loo0ng-doit` | | |
| `loo0ng-graph` | | |
| `loo0ng-domain` | | |
| `loo0ng-filing` | | |
| `loo0ng-to-docx` | | |

（`ask-loo0ng` 还没做出来，没有它是对的，不用找。）

## 客户端本身

| 问 | 答 | 原始回显在 |
|---|---|---|
| 打开这个文件夹时，客户端要求它是 Git 仓库吗 | | 人工 |
| 任务是不是被放进了 worktree（`pwd` 与律师的文件夹是不是同一个） | | `00-位置.txt` |
| 执行批准长什么样（一次一条？可以「全部允许」？） | | 人工 |

## 票 #41　渲染通道（这台机器的主力是 WPS）

**先填这一组：装了什么**

| 问 | 答 | 原始回显在 |
|---|---|---|
| WPS 装在哪、版本、bundle id | | `03-Word.txt` |
| Microsoft Word 装了吗（预期：没有） | | `03-Word.txt` |
| LibreOffice 装了吗 | | `03-Word.txt` |

**再填这一组：能不能被自动化驱动（不起 app 就能答，看 `06-字典.txt`）**

| # | 问 | 答 | 原始回显在 |
|---|---|---|---|
| 2 | WPS 的 `Info.plist` 里 `NSAppleScriptEnabled` 是什么 | | `03-Word.txt` |
| 2 | `sdef` 抓 WPS 的字典成功了吗；失败的话报什么 | | `06-字典.txt` |
| 2 | 字典里有哪些 save / export / convert / print 命令 | | `06-字典.txt` |
| 2 | 字典里有没有 PDF 字样 | | `06-字典.txt` |
| 3 | **字典里有没有分页强制**（`compute statistics` / `repaginate` / `pagination`） | | `06-字典.txt` |

**路线一：wpscli（WPS 自带的命令行入口，这一轮的重点）**

| # | 问 | 答 | 原始回显在 |
|---|---|---|---|
| 5 | `wpscli` 这个文件在不在 | | `07-渲染.txt` |
| 5 | `wpscli --help` / `-h` / 无参数，各吐出什么（**帮助原文照抄，这是最值钱的一份**） | | `07-渲染.txt` |
| 5 | `--convert-to pdf` 那几种写法有没有一种成了 | | `07-渲染.txt` |
| 5 | 成了的话，出的 PDF 几页；没成的话各报什么 | | `07-渲染.txt` |

**路线二：AppleScript 逐条（每条独立进程，一条挂了其余照跑）**

| # | 问 | 答 | 原始回显在 |
|---|---|---|---|
| 1 | `activate` 认不认（第一条，认了才有下文） | | `07-渲染.txt` |
| 1 | 能不能 `open` 那份 docx；两种路径写法哪种可用 | | `07-渲染.txt` |
| 1 | 有没有文档对象模型（`count of documents`、`name of front document`） | | `07-渲染.txt` |
| 1 | 五种导出写法哪一种成了；都不成的话**各报什么原文** | | `07-渲染.txt` |
| 1 | `print front document` 走不走得通（打印管线是退路） | | `07-渲染.txt` |
| 1 | 报错里有没有「不支持」「无法识别」这类术语解析失败的字样 | | `07-渲染.txt` |
| 3 | 分页强制真跑起来返回了什么 | | `07-渲染.txt` |
| 4 | macOS 弹了什么授权框（原话） | | 人工 |
| 5 | Codex 沙箱里能不能起 WPS；不能的话报什么 | | `07-渲染.txt` |
| 附 | 样例 A 导出成功吗、几页 | | `07-渲染.txt` |
| 附 | 样例 B（多页带页脚）导出成功吗、几页；崩了的话崩的原文 | | `07-渲染.txt` |
| 附 | 有 Word 的话，Word 那两条的结果 | | `07-渲染.txt` |

## 需要在沙箱外重跑的

（哪几条是因为沙箱受限而没跑成的，列在这里。没有就写「无」。）

## 跑的过程中还注意到的别的事实

（只写事实，一条一行。没有就写「无」。）
