# 实测结论（macOS + Codex）

- 日期：
- 机器：（型号 / 芯片，`出/01-环境.txt` 里有）
- 填表的是：Mac 上的 Codex

> 填法：每行只填事实：是 / 否 / 报错原文 / 逐字抄回的字符串。
> 填不出来的写「未跑到（卡在第 N 步：…）」。不要留空，不要下结论，不要写建议。
> 任何路径里的家目录写成 `~`。

## 票 #40　安装链与 `$` 提及形态

| # | 问 | 答 | 原始回显在 |
|---|---|---|---|
| 1 | macOS 版本、芯片 | | `01-环境.txt` |
| 1 | `python3` 路径与版本 | | `01-环境.txt` |
| 2 | `python-docx` 能 import 吗 | | `02-依赖.txt` |
| 2 | `PyMuPDF` 能 import 吗 | | `02-依赖.txt` |
| 2 | 不能的话，装的时候发生了什么（挂起？报错？） | | |
| 3 | Word for Mac 版本与构建号 | | `03-Word.txt` |
| 4 | **走通的是哪一级路线**（抄 `04-安装.txt` 末尾那行） | | `04-安装.txt` |
| 4 | 第 1 级 `npx skills add` 结果（成功 / 卡交互 / 凭据失败 / 超时） | | `04-安装.txt` |
| 4 | 第 2 级 `gh clone` 走没走（本机 gh 登录了吗） | | `04-安装.txt` |
| 4 | 第 3 级拷目录走没走 | | `04-安装.txt` |
| 4 | 装到了哪个目录 | | `04-安装.txt` |
| 5 | 清单里出现的六件 skill，**逐字**抄在下面 | 见下表 | `05-清单.txt` |
| 6 | 三件编排 skill（`loo0ng-setup-case`、`loo0ng-doit`、`ask-loo0ng`）在清单里吗 | | `05-清单.txt` |
| 7 | TUI 打 `$` 之后补全列表显示什么（律师原话） | | 人工 |
| 7 | 打完整一个名字加「你好」，有反应吗 | | 人工 |

**第 5 问的逐字抄写**（一个字符都不要改）：

| 开发机上的名字 | 这台 Mac 上清单里的原样（逐字，含冒号前的部分） |
|---|---|
| `loo0ng-setup-case` | |
| `loo0ng-doit` | |
| `loo0ng-graph` | |
| `loo0ng-domain` | |
| `loo0ng-filing` | |
| `loo0ng-to-docx` | |

（`ask-loo0ng` 还没做出来，清单里没有它是对的，不用找。）

## 票 #41　Word 渲染通道

| # | 问 | 答 | 原始回显在 |
|---|---|---|---|
| 1 | `osascript` 驱动 Word 导出 PDF：通 / 不通 | | `07-渲染.txt` |
| 1 | 不通时的完整报错 | | `07-渲染.txt` |
| 2 | 字典里有没有 `save as … file format format PDF` | | `06-字典.txt` |
| 2 | 哪种路径写法可用（HFS / POSIX / 都不行） | | `07-渲染.txt` |
| 3 | **有没有 `compute statistics`（对应 `ComputeStatistics(2)` 的分页强制）** | | `06-字典.txt`、`07-渲染.txt` |
| 3 | 有的话它返回了什么；没有的话 `repaginate` 行不行 | | `07-渲染.txt` |
| 4 | macOS 弹了什么授权框（原话） | | 人工 |
| 5 | Codex 沙箱里能不能起 Word；不能的话报什么 | | `07-渲染.txt` |
| 附 | 样例 A 导出成功吗、几页 | | `07-渲染.txt` |
| 附 | 样例 B（多页带页脚）导出成功吗、几页；崩了的话崩的原文 | | `07-渲染.txt` |

## 需要在沙箱外重跑的

（哪几条是因为沙箱受限而没跑成的，列在这里。没有就写「无」。）

## 跑的过程中还注意到的别的事实

（只写事实，一条一行。没有就写「无」。）
