---
"loo0ng-skills": minor
---

新增参考 skill `loo0ng-graph`：图引擎 CLI（`scripts/graph.py`，标准库零依赖），`图.json` 的唯一写入口。起手三选一、构成操作（节点增改序移与空白模板、模块增改名序与删空模块）、条目只追加（生成 / 确认 / 不适用，来源由动作推出）、状态只算不存、模块级不适用展开、严格惰性带入、引擎拒写（已确认不能不适用、不合校验拒写、原子写）；每次写图后覆盖重算 `图视图.md` 与 `图视图.json`（来源、前方、时限按 id 从领域图查出，两份 JSON 共用格式版本 1）。`references/格式.md` 定稿两份 JSON 的字段表与最小样例。另交付合成小领域 `evals/领域/菜园/` 与种子「图引擎」、用例「改标题」。
