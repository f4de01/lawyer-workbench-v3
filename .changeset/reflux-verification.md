---
"loo0ng-skills": minor
---

回流走通并有了验收（#34，ADR-0012）：`skills/loo0ng-domain` 的 `from-case` 加 `references/回流.md` 定的那条路，现在有种子与两侧 eval 兜着。新增种子「回流」：在种子「在办中」之上加两个律师自加节点（一个已确认、一个只生成，标题都带着本案的当事人与日期），另在工作区根摆出开发会话看得见的另两样——破产领域图的可写副本与案件图的 sha256 指纹；副本而不是 `skills/` 下那份原件，一次 eval 不该动到仓库里的领域图（ADR-0015）。新增用例「回流」：开发者一句话给案件工作区的路径，两侧都是裸提示词，断言回显清单标出了去案件化后的标题、领域图恰好多出那一个节点、id 与案件图一致、条目为空、只生成没拍板的那个没回流、案件图字节不变、雏形文件没落进工作区。`tests/loo0ng-domain/test_seeds.py` 机械复核种子状态与 `from-case` 恰好提出已确认的那一个。分支加 PR 那一道不进 eval：隐私钩子在 `main` 上守领域图由 `tests/privacy-check` 覆盖，第二双眼是另一会话的 AFK 代理审 diff。
