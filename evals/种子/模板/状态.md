# 种子「模板」的状态

在种子「图引擎」的状态之上（合成小领域「菜园」整份起手、工作区指针块），多一个 `模板/官方/`，里面只有一件官方模板原件 `1-2.关于管理人印章备案的报告.docx`（从仓库的领域资产拷来，模板本身不含案件内容）。没有 `收件箱/`、`材料/`、`文书/`，没有律师陈述。

供 skill "loo0ng-to-docx" 的用例：律师给一段最小集稿子与模板名，断言 `文书/` 下出现通过门禁的 docx、回复里有门禁结论。
