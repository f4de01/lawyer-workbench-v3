# 种子「图引擎」的状态

按合成小领域「菜园」（`evals/领域/菜园/`）整份起手：4 模块 9 节点全部在图里、都未生成、没有条目；`图视图.json` 的前方为空，3 个节点在视图里带时限句。工作区根有 `AGENTS.md`（领域目录绝对路径、图与视图文件名、入口名、只读声明）与一行 `@AGENTS.md` 的 `CLAUDE.md`，照 ADR-0009 机制 B 的块，起手 skill 落地前由 `回放.py` 代写。

没有收件箱文件、没有文书、没有律师陈述。供 skill "loo0ng-graph" 的用例：律师一句话改构成，断言 `图.json` 与两份视图。
